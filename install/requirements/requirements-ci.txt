# iTaK CI - minimal deps for testing only
# Full requirements.txt has heavy native packages (browser-use, playwright,
# weaviate-client, neo4j, discord.py) that need special CI setup.
# Tests in tests/test_core.py only need lightweight core modules.

# Core (needed by imports in test files)
pydantic>=2.0.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0

# WebUI (test_webui imports)
fastapi>=0.115.0,<1.0.0

# Memory (SQLite store tests)
aiosqlite>=0.20.0,<1.0.0
numpy>=1.26.0,<3.0.0

# Logging
structlog>=24.0.0,<26.0.0

# Security (for doctor checks)
cryptography>=46.0.5,<47.0.0

# Search (httpx used by various modules)
httpx>=0.27.0,<1.0.0

# Testing
pytest>=8.0.0,<9.0.0
pytest-asyncio>=0.24.0,<1.0.0

# Linting
ruff>=0.7.0,<1.0.0
